"""Conversation agents (brains). Transport-agnostic: they consume text turns and
emit text + structured events, so the same brain runs over browser speech, the
local PCM pipeline, or the canned demo."""
